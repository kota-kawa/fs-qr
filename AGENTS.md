# リポジトリガイドライン

## エージェントの対応言語

利用者への説明、作業・編集途中の経過報告、最終報告はすべて日本語で表示してください。コード、
識別子、ログ文言など英語表記が定められているものを除き、利用者に向けたテキストは日本語を基本と
します。ソースコードのコメントは英語または日本語で読み手に意図が伝わるように書き、既存ファイルでは
周囲のコメントの言語に合わせます。

## ドキュメントの参照先

各行は「パス／内容の区分／いつ参照するか、または何に使わないか」の型で書きます。詳細の要約や
特定機能に紐づく記述は書きません。

- `README.md` は公開向けの概要・機能紹介・起動手順です。内部設計の正本としては扱わず、実装との差分が見つかった場合は公開向け説明として修正します。
- `ARCHITECTURE.md` はシステム構成、責務、リクエスト経路、データ・契約の境界をまとめた内部設計の正本です。複数レイヤーにまたがる変更では最初に必要な章だけ参照してください。
- `docs/knowledge/development_conventions.md` はプロジェクト構成、ビルド・テストコマンド、スキーマ同期、実装規約、命名規則、責務分割、テスト方針、サブエージェントの指定をまとめた開発規約です。コードを変更する作業では着手前に必ず読んでください。
- `docs/knowledge/README.md` は再利用可能な知見の索引です。デバッグ手順は `docs/knowledge/debugging.md`、API 契約とマイグレーションの注意点は `docs/knowledge/contracts-and-migrations.md`、worktree を分けた後の運用手順は `docs/knowledge/parallel_work.md`、フロントエンドの実描画確認は `docs/knowledge/frontend_visual_verification.md`、過去に繰り返した回帰の型は `docs/knowledge/recurring_regressions.md`、リアルタイム機能は `docs/knowledge/realtime.md`、共通部品の拡張規則は `docs/knowledge/shared-service-components.md` を参照してください。作業ログや一時的な状態は追加しません。
- `docs/decisions/README.md` は重要な技術判断（ADR）の索引です。判断を変更・追加するときは、既存 ADR を確認して理由と影響を更新・記録してください。
- `docs/blue-green-deploy.md` は本番の初回セットアップ、切替、sudo、rollback、手動デプロイの運用手順です。構成の理解には使わず、`ARCHITECTURE.md` から辿ってください。
- `docs/eu-readiness.md` は EU 対応のデータ取扱い、公開告知、cookie / 広告、既知 gap の前提資料です。法務・プライバシー文書を変更するときに参照し、実装の責務確認には使いません。
- `docs/screenshots/` は PR や公開説明で参照するスクリーンショットの置き場です。設計や手順の確認には使いません。
- `locales/README.md` は翻訳カタログの分割、キー、phrases shard、Babel カタログ生成、検証方法です。翻訳の更新先はこの文書だけとし、他の文書に翻訳手順を重複記載しません。
- `spec.md` は画面文言、エラー表示、モーダルの使い分けなど UI 上の規約です。UI 文言やエラー表示を変更するときに参照し、内部設計の確認には使いません。
- `.github/BRANCH_PROTECTION.md` は `main` の保護設定と必須 CI チェックの記録です。マージ条件や CI ゲートを確認・変更するときに参照してください。
- `CLAUDE.md` は `AGENTS.md` を読むための委譲ファイルです。新しい作業ルールをここへ重複記載しません。

ドキュメントを追加・削除したら、同じコミットでこの一覧と `docs/knowledge/README.md` または
`docs/decisions/README.md` の索引も更新してください。文書内のパス参照が存在するかは
`python3 scripts/check_doc_paths.py` が CI で検証します。

## コミットおよびプルリクエストのガイドライン

- `main` ブランチ上でファイルを修正したり、`main` へ直接 push したりしてはいけません。すべての変更は `feature/<内容>`、`fix/<内容>`、`docs/<内容>` のような目的が分かる作業ブランチにコミットし、必ず PR（プルリクエスト）を作成して `main` に取り込んでください。
- コミットメッセージは PR タイトルと同じく「日本語の要約 / English summary」の併記で、短くアクション指向にしてください。厳格な conventional-commit 接頭辞は使いません。本文には変更理由と副作用を書きます。
- 1 つの PR に複数の種類の変更（例: バグ修正とリファクタリング、機能追加と無関係な整形）が含まれる場合は、1 つのコミットにまとめず、変更の種類ごとに複数のコミットに分けてください。レビュー、`git bisect`、部分的な revert のしやすさを優先します。
- PR には次を含めます: 明確な説明、関連 Issue へのリンク（存在する場合）、設定・マイグレーションに関する注意事項、テストに関するメモ（実行したコマンドと結果）、未実施の検証とその理由。見た目に影響する変更では `docs/knowledge/frontend_visual_verification.md` の確認結果を書きます。スクリーンショットは必須ではありませんが、大規模な UI 変更では添付を推奨します。添付する場合は `docs/screenshots/<話題>/` にコミットし、PR 本文から参照してください。
- PR のタイトルと本文は日本語と英語の両方で作成してください（PR titles and descriptions must include both Japanese and English）。本文は `## 日本語` と `## English` の見出しで分け、同等の情報を記載します。
- エージェントは、ユーザーから明示的な指示がない限り PR をマージしてはいけません。
- `main` へのマージは、直前のマージで起動した CI の実行が完了してから行ってください。CI の concurrency group は `cancel-in-progress: true` のため、連続してマージすると直前の `main` コミットの実行が打ち切られ、そのコミットに対する検証結果が残りません。本番デプロイは手動実行・承認制（ADR-0007）なので自動では進みませんが、デプロイ対象に選ぶコミットは CI が完走したものにしてください。

## エージェントの作業ルール

- 作業開始前に現在のブランチと未コミットの変更を確認してください。すでに今回の作業専用ブランチにいる場合は、新たなブランチを重ねて作成する必要はありません。
- ユーザーまたは他のエージェントによる既存の変更を、明示的な許可なく上書き、破棄、または巻き戻してはいけません。競合する場合は作業を止めて確認します。
- 要件が曖昧なまま実装コードを書かないでください。着手前に質問してスコープとスコープ外を合意し、合意していない変更は加えません。未使用のヘルパー、過剰な抽象化、コメントアウトした残骸などの不要なコードも生成しません。
- コメント、docstring、関数名を仕様の根拠にせず、実装本体とテストを読んで判断してください。触った範囲で実装と食い違うコメントや名前を見つけたら、同じ変更で直します。
- テスト失敗やレビュー指摘に対する自動修正のループは 5 回を上限とします。超えたら自力で続けず、何が未解決でどこで詰まっているかを添えてユーザーに報告してください。
- 実装したエージェントは自分で完了判定をしないでください。コードを変更する PR は、作る前にレビュー用サブエージェント（指定は `docs/knowledge/development_conventions.md` の「サブエージェントの指定」）へスコープ・差分・チェックリスト（要件を満たす／スコープ外の変更が無い／不要なコードが無い／テストが通る／禁止事項に触れない）だけを渡してレビューさせ、実装担当の意図や経緯の説明は渡しません。ドキュメントだけの変更はこのレビューを省略でき、省略した場合は PR 本文にその旨を書きます。指摘は検証してから反映します。指摘に反論がある場合は握りつぶさずユーザーに上げてください。
- 修正後は、変更箇所および直接影響を受ける範囲に対応するテストのみを実行してください。変更と関係のないテストやテストスイート全体は実行しません。ただし、変更が横断的で影響範囲を限定できない場合、またはユーザーが明示的に求めた場合はこの限りではありません。テストを実行できない場合は、その理由を最終報告と PR 本文に記載してください。
- 見た目や操作に影響するフロントエンド変更（CSS、テンプレートの構造、静的 JS のイベント処理）は、PR を作る前に Playwright で実描画・実操作を確認し、取得したスクリーンショットを実際に目視して、レイアウト崩れ、文字切れ、重なり、余白、サービス（FSQR / Group / Note / Task）間の配色差異がないことを確認してください。確認した画面・ビューポート（PC とスマホ幅）・実測結果を PR 本文に書きます。手順は `docs/knowledge/frontend_visual_verification.md` にあります。API クライアントやサーバー側のみの変更など描画に出ない変更は対象外です。
- 共通テンプレート・共通 JS・共通 CSS を変更するときは、4 サービスすべての入口ページ、ルーム画面、LP を確認対象に含めてください。共通化で起きた回帰の型は `docs/knowledge/recurring_regressions.md` にあります。
- サブエージェントと git worktree は別の判断軸です。「作業を独立分割できるか」でサブエージェントを、「作業ツリーを共有すると壊れるか」で worktree を判断してください。
- サブエージェントを使う: 読み取りのみの調査・レビュー、または担当ファイルを重複なく分割できる編集。調査・実装・レビューを分けるときは役割ごとに別の担当にし、各担当にはその役割に必要な情報だけを渡し、原則として同じファイルを同時に編集させないでください。複数の担当に分けるときは、着手前に「誰の成果物を誰にどの形式で渡すか」「何が揃ったら次を始めてよいか」「最終判定は誰か」を決めて各担当の依頼文に書きます。最終判定はユーザーで、エージェント間の判断の不一致はユーザーに上げます。
- worktree を使う（いずれか該当時）: 並列作業が同じ生成物（`static/js/note_room_realtime/yjs-collaboration.js`、`locales/*/LC_MESSAGES/messages.po`、alembic の head、`hocuspocus/package-lock.json`、`db_init/create_tables.sql` など）に触る／別 PR に分けるべき変更を同時に進める／破棄する可能性のある大規模リファクタ／作業ツリーにユーザーの未コミット変更がある。
- worktree を使わない: 数ファイル規模の変更と、`python3 app.py` や `docker-compose up` の起動が必要な作業。`.env` は git 管理外のため新規 worktree では起動できません。
- 並行数の上限は worktree の本数ではなく「担当ファイルが重複しないこと」で決めてください。同時に走る作業どうしが同じファイルを編集する予定なら、worktree を増やしても解決しないので片方を待たせます。worktree を分ける単位は着手前に決め、実装途中で思いつきで分割しないでください。並列作業中は担当 worktree の外を触らず、他の作業への依存が判明したら止めてユーザーに報告します。1 worktree = 1 ブランチ = 1 PR とし、共有される DB／ポート、`node_modules`、生成物の再生成、依存のある作業の順序などの運用手順は `docs/knowledge/parallel_work.md` に従います。
- データベーススキーマを変更する場合は、新しい Alembic migration、`db_init/create_tables.sql` の更新、スキーマ整合性の回帰テストを同じ変更で追加してください。適用済みまたは既存の migration ファイルを書き換えてはいけません。Blue-Green 切替中は旧コードと新スキーマが共存するため expand / contract で設計します。詳細は `docs/knowledge/contracts-and-migrations.md` にあります。
- 新しい依存関係を追加する前に、その必要性と既存の依存関係で代替できないことを確認してください。追加した場合は、理由と影響を PR に記載し、`requirements.txt` / `requirements-dev.txt` / `hocuspocus/package.json` のピン留めと `.github/workflows/tests.yml` のバージョンを一致させてください。`pip-audit`、`npm audit`、Trivy を通ることも確認します。
- 依頼された目的に必要な範囲だけを変更し、無関係なリファクタリングや整形を同じ変更に含めないでください。

## セキュリティと設定のヒント

- 必要な環境変数には MySQL の接続設定（`SQL_HOST`、`SQL_USER`、`SQL_PW`、`SQL_DB`）、`SECRET_KEY`、`ADMIN_KEY`、`MANAGEMENT_PASSWORD`、`DB_ADMIN_PASSWORD`、`REDIS_URL`、`PUBLIC_SITE_URL` が含まれます。`settings.py` は placeholder のままの秘密値では起動を拒否します。シークレット情報は環境変数または `.env` に保持し、git には含めないでください。
- 実行時に読まれる環境変数の一覧は `.env.example` が正本です。`python3 scripts/check_env_documentation.py` が、コードの読み取りと `.env.example` の記載の同期を CI で検証します（環境変数を追加・削除したら同じ変更で `.env.example` も更新してください）。Docker では `docker-compose.yml` の `environment` に列挙した変数だけがコンテナへ届くことにも注意します。
- `.env` には秘密情報が入るため、その内容を読んだり、ログ・文書・報告へ転記したりしてはいけません。環境変数の一覧を確認するときは `.env.example` を読んでください。
- アップロード可能なファイル数と合計サイズは `settings.py` の `UPLOAD_MAX_FILES` と `UPLOAD_MAX_TOTAL_SIZE_MB` に対応する環境変数で制限します。制限を変更する場合は、`.env.example`、`README.md`、`docker-compose.yml`、`fs-qr.conf` の `client_max_body_size`、サーバー側とクライアント側の検証へ一貫して反映してください。
- 認証、セッション、WebSocket、ファイルアップロードを変更する場合は、CSRF 対策、認可、ファイル名の無害化、MIME タイプと内容の検証、サイズ制限、パストラバーサル対策への影響を確認してください。セキュリティ上の検証を無効化したり、シークレットや個人情報をログへ出力したりしないでください。認証情報を含む legacy URL や共有 token は `log_config.py` と nginx で伏せられているため、テスト出力や報告にもそのまま残さないでください。
